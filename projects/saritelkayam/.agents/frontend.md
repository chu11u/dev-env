# Agent: Frontend

## Role

Build all page components, page sections, and routing for the Next.js 15 application. Use the design system components built by the Design System agent, and compose them into complete pages.

## Model

`qwen3.6:27b-coding-nvfp4`

## Skills to Load

- `.skills/nextjs-app-router.md` — Next.js 15 patterns, App Router conventions
- `.skills/tailwind-css.md` — Tailwind utility patterns, responsive design
- `.skills/framer-motion.md` — Animation patterns (Phase 3 polish)

## Scope Boundaries

### Owns (writes these files)
- `frontend/app/page.tsx` — Home page
- `frontend/app/services/page.tsx` — Services page
- `frontend/app/testimonials/page.tsx` — Testimonials page
- `frontend/app/shop/page.tsx` — Products/shop page (placeholder, Stripe on hold)
- `frontend/app/book/page.tsx` — Booking page (placeholder)
- `frontend/app/blog/page.tsx` — Blog listing
- `frontend/app/blog/[slug]/page.tsx` — Individual blog post (reads markdown files)
- `frontend/app/contact/page.tsx` — Contact page
- `frontend/app/not-found.tsx` — 404 page
- `frontend/app/manifest.ts`, `sitemap.ts`, `robots.ts` — SEO files
- `frontend/components/sections/` — Page-specific section components
  - `HeroSection.tsx` — Home page hero
  - `ServicesPreview.tsx` — Home page services preview
  - `TestimonialsSection.tsx` — Home page testimonials
  - `CTASection.tsx` — Call-to-action sections
- `frontend/lib/` — Frontend utilities
  - `blog.ts` — Markdown file reading, slug extraction
  - `mdx.ts` — MDX rendering (if used)

### Reads (do not modify)
- `MEMORY.md` — Site map, feature descriptions
- `.skills/` — Reference patterns
- Design system components (`frontend/components/ui/`, `frontend/components/layout/`)
- Root layout (`frontend/app/layout.tsx`) — Created by Design System agent
- Media assets (`frontend/public/assets/`) — Created by Media agent
- Blog markdown content (`frontend/public/content/`) — Created by Fullstack agent

### Must NOT Touch
- Design system files (`frontend/components/ui/`, `frontend/components/layout/`) — Design System agent
- Tailwind config, global CSS — Design System agent
- Docker files, nginx config — Infrastructure agent
- Backend API routes, Prisma schema — Fullstack agent
- Admin CMS (`frontend/app/admin/`) — Fullstack agent

## Task Queue

### Phase 2 Tasks (execute in order)

1. **Build Home page (`/`)**
   - Hero section with headline, subheadline, CTA button, background image
   - Services preview (3 featured services with cards)
   - Testimonials section (customer quotes)
   - CTA section (book appointment / contact)

2. **Build Services page (`/services`)**
   - Full service catalog with pricing
   - Service cards with descriptions, duration, price
   - Categories if applicable

3. **Build Testimonials page (`/testimonials`)**
   - Customer reviews with photos (placeholder avatars)
   - Rating stars, service type badges
   - Grid layout

4. **Build Contact page (`/contact`)**
   - Contact form (name, email, phone, message)
   - Location info (if available)
   - Social media links

5. **Build Blog pages**
   - Blog listing (`/blog`) — Grid of posts with featured images
   - Blog post (`/blog/[slug]`) — Markdown rendering from `frontend/public/content/`
   - Dynamic route params, not-found handling for missing slugs

6. **Build placeholder pages**
   - Shop (`/shop`) — "Coming soon" placeholder
   - Booking (`/book`) — "Coming soon" placeholder

7. **Build 404 page**
   - Elegant not-found page matching brand style

8. **Build SEO files**
   - `manifest.ts` — Web app manifest
   - `sitemap.ts` — Dynamic sitemap
   - `robots.ts` — Allow all crawlers

### Phase 3 Tasks (polish)

9. **Add Framer Motion animations**
   - Fade-in on scroll for sections
   - Subtle hover effects on cards
   - Page transition animations
   - Follow `.skills/framer-motion.md` patterns

10. **Mobile responsive polish**
    - Test all breakpoints (sm, md, lg, xl)
    - Mobile menu functionality
    - Touch-friendly interactions

## Output Expectations

After Phase 2:
- All pages render correctly ✅
- Navigation works between all pages ✅
- Blog reads markdown files from disk ✅
- Placeholder pages exist for deferred features ✅
- SEO files generate correctly ✅

After Phase 3:
- Animations are subtle and performant ✅
- All pages look great on mobile ✅
- No layout shifts or jank ✅

## Constraints

- Use design system components exclusively — do not create duplicate UI components
- All images reference files from `frontend/public/assets/` (generated by Media agent)
- Blog content reads from `frontend/public/content/` (markdown files)
- Keep page components clean — extract reusable sections into `components/sections/`
- Use Next.js Image component for all images (optimized loading)
